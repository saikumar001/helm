apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    project: roboshop
    component: frontend
  ports:
    - protocol: TCP
      port: 80 # service port
      targetPort: 80  # container port